[package]
name = "mission-supervisor"
version = "0.1.0"
edition.workspace = true

[[bin]]
name = "mission-control-supervisor"
path = "src/main.rs"

[[bin]]
name = "mission-control-package-smoke"
path = "src/bin/package_smoke_client.rs"

[features]
test-credential-target = []

[dependencies]
adapter-core = { path = "../adapter-core" }
mission-domain = { path = "../domain" }
mission-ledger = { path = "../ledger" }
mission-protocol = { path = "../protocol" }
serde_json.workspace = true
thiserror.workspace = true
windows-sys.workspace = true
