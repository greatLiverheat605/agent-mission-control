# Space Scene Assets

P5 currently ships no external bitmap, HDR, or texture asset.

The starfield is generated deterministically from seed `0x5f3759df` at runtime using Three.js buffer geometry. Mission nodes, routes, branches, and the Agent marker use code-native geometry and the audited CSS status tokens. This avoids runtime network requests and unverified asset licenses.
