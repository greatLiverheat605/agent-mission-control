{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window access for the local supervisor status shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
